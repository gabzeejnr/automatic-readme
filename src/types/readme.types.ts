export type ReadmeData = {
    name: string,
    description?: string | undefined,
    url?: string,
    framework: string,
    techStack: string[],
    routes: string[],
    apiRoutes: string[],
    scripts: {}[]
}